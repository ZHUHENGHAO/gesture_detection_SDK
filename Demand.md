1. 创建imu_task，用于读取IMU数据；创建algo_task，用于算法处理接收到的IMU数据；
   使用SDK提供的IMU HAL接口，在imu_task里实现IMU传感器的初始化和配置，将IMU的采样率配置为50Hz；
2. 通过IMU中断处理函数触发获得IMU的data ready的信号，在imu_task里读取IMU数据并暂时存储到RAM里；
   imu_task里接收到足够量的IMU数据后，发送Event到algo_task请求进行算法处理；
3. 首次读到320000Byte IMU数据时，使用CRC校验算法得到CRC值（即计算320000Byte数据的CRC值），并通过printf接口打印出该值；
4. 在algo的manager里处理Event，调用自己实现的算法接口对IMU数据进行处理；识别到有效的手势后通过printf接口打印时间和手势结果。



**要求:**

1. ##### **在嵌入式开发中，中断处理函数（ISR）中禁止执行以下操作：**

    调用IMU的HAL接口，

    调用非ISR安全的OS接口及Event接口，

    执行CRC算法运算，

    调用printf接口

2. ##### **嵌入式开发的消息需要通过Event传递;**
   
3. ##### **手势识别算法输出结果格式：**
    输出结果的打印格式统一为“时间, 手势结果”（多个结果换行输出）。
    当识别到双指互点、握拳、抬腕、放下手势时，进行结果打印，四种手势的打印内容分别为“pinch”、“clench”、“up”、“down”。
    当识别到其它动作时，不进行结果打印。
    打印结果时，应同步统计当前算法累计已处理的IMU数据量，并根据实际采样率、通道数、数据位宽转换为时间，以毫秒显示。
    例如，当前处理数据量为700字节，假设当前采样率为50Hz、通道数为7、位宽为2字节（int16类型，实际配置以初赛SDK开发手册为准），则当前处理了700/2/7/50*1000=1000毫秒的数据；并且假设当前识别结果为“clench”；则结果应显示为“1000ms, clench”。

4. ##### **算法处理函数内部禁止直接打印结果；识别结果应以返回值或回调的形式传递给非算法模块，统一进行结果打印。**

5. ##### **设备启动即开始运行算法，算法处理延迟需严格控制，从手势动作结束到上层输出结果的总延迟应越低越好，一般建议延迟不超过2秒。**

6. ##### **算法需要在VeriHealthi SDK中完成验证，算法模型的资源占用（包括Flash和所有RAM）必须严格控制在SDK链接文件设定的范围内。**
